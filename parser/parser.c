#include "parser.h"

bencode_pairs_list parse_file_content_buffer(file_content_buffer buffer)
{
    bencode_pairs_list pairs_list;    
    int i = 0;
    while(i < buffer.size)
    {
        switch (buffer.data[i])
        {
            case 'd':
        }
    }
}
